[options]
HoldPkg     = pacman glibc
Architecture = aarch64
SigLevel    = Never TrustAll
LocalFileSigLevel = Never

[core]
Include = /etc/pacman.d/mirrorlist

[extra]
Include = /etc/pacman.d/mirrorlist

[community]
Include = /etc/pacman.d/mirrorlist

[alarm]
Include = /etc/pacman.d/mirrorlist

[aur]
Include = /etc/pacman.d/mirrorlist

[qcom]
Server = https://github.com/silime/ArchLinux-Packages/releases/latest/download/